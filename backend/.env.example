# Google Gemini API Key (必填)
# 获取地址: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# 管理员密码 (必填，用于访问 swagger 文档和生成激活码)
ADMIN_PASSWORD=change_this_password

# 管理员用户名 (选填，默认 admin)
ADMIN_USERNAME=admin

# 允许跨域的域名 (选填，默认 http://localhost, 多个域名用逗号分隔)
# 生产环境请修改为您的实际域名，如: http://ai.778ba.com
CORS_ORIGINS=http://localhost,http://127.0.0.1,http://ai.778ba.com

# 数据库文件路径 (选填)
# DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# 数据保留时间 (小时，选填，默认 24)
# DATA_RETENTION_HOURS=24

# 是否开启 API 文档 (选填，默认 false)
ENABLE_DOCS=false

# Gemini 模型设置
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_TEMPERATURE=0.7
# GEMINI_ENABLE_THINKING=false
