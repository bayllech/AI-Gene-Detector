# 环境配置（请复制此文件为 .env 并填写真实值）

# Google Gemini API 配置
# 获取地址: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# 模型名称（默认使用 gemini-3-flash-preview）
GEMINI_MODEL=gemini-3-flash-preview

# 生成参数（用于对齐 AI Studio / 提升坐标稳定性）
# 温度越低越“稳定”（推荐先试 0-0.3），越高越“发散/幽默”
GEMINI_TEMPERATURE=1.0

# 是否启用 Thinking（可能提升定位稳定性，但也可能增加延迟）
GEMINI_ENABLE_THINKING=true

# 数据库配置 (默认使用 SQLite)
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# 服务器配置
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS 允许的源 (前端地址，多个用逗号分隔)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 数据保留时间（小时）
DATA_RETENTION_HOURS=24

# 临时文件存储路径
TEMP_STORAGE_PATH=./data/temp
